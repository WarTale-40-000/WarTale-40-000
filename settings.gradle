pluginManagement {
    repositories {
        maven {
            name = 'AzureDoom Maven'
            url = uri("https://maven.azuredoom.com/mods")
        }
        gradlePluginPortal()
    }
}

rootProject.name = "Wartale"
